[project]
name = "uv-practice"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "大島新太", email = "sh-oshima@brexa.com" }
]
requires-python = ">=3.12"
dependencies = [
    "jupyter>=1.1.1",
    "pytest>=9.1.1",
    "requests>=2.34.2",
]

[project.scripts]
uv-practice = "uv_practice:main"

[build-system]
requires = ["uv_build>=0.12.11,<0.13.0"]
build-backend = "uv_build"
